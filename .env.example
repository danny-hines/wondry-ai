# Copy to .env and fill in to enable live (non-mock) generation.
# Leave unset to run fully on the keyless mock provider.
ANTHROPIC_API_KEY=
# Optional: set to route any task/tier at the 'openai' provider (see config.json).
OPENAI_API_KEY=
PORT=8080
ADMIN_PASSWORD=wondry
