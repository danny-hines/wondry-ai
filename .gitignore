# Secrets / environment — never commit real keys (ANTHROPIC_API_KEY, ADMIN_PASSWORD)
.env
.env.local
*.env.local

node_modules/
data/*.db
data/*.db-*
artifacts/*.html
artifacts/*.json
artifacts/media/
*.log

# Piper TTS (downloaded/created by: npm run setup-piper)
voices/
vendor/
.venv-piper/

# Saved audio/camera device prefs (written by: wondry audio)
.kiosk-devices

# React client (Vite)
client/node_modules/
client/dist/
client/*.timestamp-*.mjs

# OS cruft
.DS_Store
Thumbs.db
